substitutions:
  device_name: esp32general
  friendly_name: esp32general
  esp32_board: esp32dev
  relay_pin:
    number: GPIO23
    inverted: true
  led_pin: GPIO5
  button_pin: GPIO19
  localdomain: ".local"

packages:
  base: !include base-brainplug-esp32.yaml

api:
  encryption:
    key: !secret esp32general__encryption_key
  reboot_timeout: 0s